arb: |
  "{{feature.camelCase()}}{{slice.pascalCase()}}Title": "{{feature.titleCase()}} {{slice.pascalCase()}}",
  "{{feature.camelCase()}}{{slice.pascalCase()}}EmptyTitle": "No {{feature.titleCase()}} {{slice.pascalCase()}} Found",
  "{{feature.camelCase()}}{{slice.pascalCase()}}EmptyMessage": "No {{feature.titleCase()}} {{slice.pascalCase()}} found at the moment",
  "{{feature.camelCase()}}{{slice.pascalCase()}}ErrorTitle": "Failed to Load {{feature.titleCase()}} {{slice.pascalCase()}}"

export:
  ui: |
    export 'ui/{{slice.snakeCase()}}/views/{{feature.snakeCase()}}_{{slice.snakeCase()}}_view.dart';
    export 'ui/{{slice.snakeCase()}}/widgets/{{feature.snakeCase()}}_{{slice.snakeCase()}}_content.dart';
    export 'ui/{{slice.snakeCase()}}/widgets/{{feature.snakeCase()}}_{{slice.snakeCase()}}_empty_feedback.dart';
    export 'ui/{{slice.snakeCase()}}/widgets/{{feature.snakeCase()}}_{{slice.snakeCase()}}_error_feedback.dart';
    export 'ui/{{slice.snakeCase()}}/widgets/{{feature.snakeCase()}}_{{slice.snakeCase()}}_skeleton.dart';

post_hooks:
  - flutter gen-l10n