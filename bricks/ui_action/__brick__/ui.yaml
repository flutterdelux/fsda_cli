arb: |
  "{{feature.camelCase()}}{{slice.pascalCase()}}Action": "{{slice.titleCase()}}",
  "{{feature.camelCase()}}{{slice.pascalCase()}}Success": "{{slice.titleCase()}} {{feature.titleCase()}} successfully",
  "{{feature.camelCase()}}{{slice.pascalCase()}}OverlayMessage": "{{slice.titleCase()}} {{feature.titleCase()}}...",

export:
  ui: |
    export 'ui/{{slice.snakeCase()}}/widgets/{{feature.snakeCase()}}_{{slice.snakeCase()}}_button.dart';

post_hooks:
  - flutter gen-l10n
