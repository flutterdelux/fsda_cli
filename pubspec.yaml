name: fsda_cli
description: Feature Slice Driven Architecture CLI for workspace scaffolding and code generation.
version: 1.0.8
repository: https://github.com/flutterdelux/fsda_cli
license: BSD-3-Clause

environment:
  sdk: '>=3.10.7 <4.0.0'

dependencies:
  analyzer: ^12.1.0
  args: ^2.7.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  mason: ^0.1.2
  mason_logger: ^0.3.5
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  build_runner: ^2.15.0
  freezed: ^3.2.5
  json_serializable: ^6.14.0
  lints: ^6.0.0
  test: ^1.25.6

executables:
  fsda: fsda_cli
