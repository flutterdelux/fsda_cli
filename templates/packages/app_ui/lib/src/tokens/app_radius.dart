class AppRadius {
  // --- Base Scale ---

  /// Very extra small radius (2.0), used for subtle rounding on small elements or accents.
  static const xxs = 2.0;

  /// Extra small radius (4.0), used for subtle rounding on small elements or accents.
  static const xs = 4.0;

  /// Small radius (8.0), typically used for small components like tags or chips.
  static const sm = 8.0;

  /// Medium radius (16.0), used for medium-sized elements.
  static const md = 16.0;

  /// Large radius (24.0), used for prominent containers.
  static const lg = 24.0;

  /// Extra large radius (32.0), used for very rounded sections or bottom sheets.
  static const xl = 32.0;

  /// Very extra large radius (48.0), used for very rounded sections or bottom sheets.
  static const xxl = 48.0;

  // --- Semantic Tokens ---

  /// Default radius for cards and main containers.
  static const card = 16.0;

  /// Default radius for dialogs and modal popups.
  static const dialog = 16.0;

  /// Default radius for buttons.
  static const button = 16.0;

  /// Default radius for input fields (TextField, Dropdown).
  static const input = 16.0;

  /// Default radius for snackbars.
  static const snackbar = 16.0;
}
